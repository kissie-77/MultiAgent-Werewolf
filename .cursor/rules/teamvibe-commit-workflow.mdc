---
description: 每次 git commit/push 必须走 TeamVibe 决策流程
globs:
alwaysApply: true
---

# TeamVibe 提交强制流程

本项目已启用 TeamVibe（个人级 Hook + 项目 `.teamwork/`）。

## 每次 `git commit` 前

1. 读取 `.teamwork/decisions/` 与 `.teamwork/drafts/current.json`
2. 回顾对话与 `git diff --cached`，识别业务决策 vs 纯技术改动
3. **禁止**直接执行 `git commit` 而不经下列之一：
   - **有业务决策**：生成决策 JSON → 展示给用户确认 → 写入 `.teamwork/decisions/` → `git add` 决策文件 → 再 commit
   - **仅 bug 修复 / 注释汉化 / 格式化 / 文档同步**：在 commit message 中加 `[skip decision]`，并在回复中说明 `noise_excluded` 理由

4. 用户确认前不得 push

## 每次 `git push` 前

- 确认每个含代码变更的 commit 都有对应决策文件，或 commit message 含 `[skip decision]`
- Hook 会校验；缺记录时补决策或修正 commit

## 决策记录格式

见 `~/.cursor/skills/teamvibe/SKILL.md` 与 `.cursor/rules/teamwork-decisions.mdc`。

## 当前用户

`kissie-77`（kissie-77@users.noreply.github.com）
