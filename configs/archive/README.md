# Archived configs

These YAML files are kept for reference only. They are **not** wired into `modes.py`, CI, or default scripts.

For active setups use `configs/example.yaml`, `configs/llm-*-doubao.yaml`, or `configs/demo-6.yaml`.
