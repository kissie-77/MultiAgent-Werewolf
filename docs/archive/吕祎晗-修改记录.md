# 吕祎晗 - 修改记录

> 给同事看的变更摘要。详细待办见 [吕祎晗-发现问题.md](./%E5%90%95%E7%A5%8E%E6%99%97-%E5%8F%91%E7%8E%B0%E9%97%AE%E9%A2%98.md)。
> 架构说明见 [工程架构重构计划.md](../architecture/工程架构重构计划.md)。
> 设计专篇：[评测模块优化设计](../architecture/evaluation/吕祎晗-评测模块优化设计.md) · [提示词版本与变量设计](../architecture/吕祎晗-提示词版本与变量设计.md) · [OpenAI 接口分析](./%E5%90%95%E7%A5%8E%E6%99%97-OpenAI%E6%8E%A5%E5%8F%A3%E4%B8%8E%E5%8F%AF%E5%88%A0%E9%99%A4%E6%80%A7%E5%88%86%E6%9E%90.md)
> 最近更新：**2026-06-02**（文档三件套归位 + Per-role 版本 + ReMe 下线）

---

## 文档与 Per-role 版本（2026-06-02）

| 项 | 说明 |
|----|------|
| `docs/evaluation/` | 仅三件套；专题迁 `architecture/evaluation/` |
| `docs/agent_team/memory/` | 记忆子模块三件套；专题迁 `architecture/memory/` |
| Per-role Prompt | `prompts/roles/<role>/<version>/`；默认 **latest** |
| Per-role Skill | `skills/<role>/<version>/`；PostGame 写前 bump |
| ReMe | 已下线；语义记忆走 Skill MD |

设计专篇见 [提示词版本与变量设计](../architecture/吕祎晗-提示词版本与变量设计.md)（evaluation 专题见 [evaluation/DESIGN.md](../evaluation/DESIGN.md)）。

---

## Evaluation v2 + Prompt 变量化 + PostGame（2026-05-25）

> 评测 **39 passed**（含 PostGame / log_views / scoring / skill / roster）。

### 1. 提示词变量化（`strategy/prompts/v2/`）

| 项                             | 说明                                           |
| ------------------------------ | ---------------------------------------------- |
| `prompt_registry.py`           | 变量 id → 外置 `.md` / `.yaml`                 |
| 外置正文                       | `v2.agent.base` + 7 角色 `v2.role.*`           |
| `PlayersConfig.prompt_version` | 已 superseded：默认 per-role **latest**（见 `RoleVersionManifest`） |
| 仍留 Python                    | `GamePrompts` / `PlanStrategies`（M2/M3 再迁） |

### 2. 评测 PostGame 流水线（`evaluation/post_game/`）

| 阶段          | 产物                                                          |
| ------------- | ------------------------------------------------------------- |
| 规则          | vote_swing、camp_persuasion                                   |
| **log_views** | `views/` + `views_manifest.json`（god / POV / digest）        |
| **scoring**   | `intention_scores.json`、`benefit_scores.json`（Phase1 部分） |
| LLM 复盘      | **`eval_agent.py` + AgentScope**（移除 `AsyncOpenAI` 旁路）   |
| Prompt 提案   | `prompt_proposals_v2` + **`target_variable`**                 |
| Skill         | `role_skills.json` + `runs/<id>/skills/*.md`                  |

### 3. Skill 生成（规则门控，**不做** benefit 分数筛选）

- **`skill_generation_rules.py`**：仅当素材通过规则才写入 `skills[]` / MD
    - 说服：`public_speech` ≥ 8 字 + 阵营匹配摇摆 ≥ 1（或命中放逐）
    - 夜间：`seer_checked` / `witch_*` / `guard_protected` / `werewolf_killed` + 有效 target
- **`agent_team/skill_loader.py`**：按 `prompt_role_key` 加载 MD，追加到系统 Prompt
- **`agent_team/skills/`**：PostGame 本地同步目录（**MD 已 gitignore**，只提交 README）

### 4. Roster 身份补全（`run_context.py`）— 2026-05-25 修复

**问题**：离线 PostGame 仅从 `player_eliminated` 反推身份 → `player_9`（预言家）误映射为 `villager`。

**修复**：从全量 `events.jsonl`（上帝视角）读取：

- `role_acting`、`player_discussion`、`role_revealed`、`player_eliminated`
- 合并 `roster_from_engine`（对局内 finalize 时）

**验证**：`runs/doubao-9p-20260525-170043` 重跑后 Skill 桶为 `prophet` / `guard`（非 villager）。

### 5. 入口与脚本

- `interface/finalize_run.py`：对局结束自动 PostGame
- `scripts/run_game_with_replay.py`：真 API 对局 + 产物导出

### 6. 队友对接要点

| 模块       | 你要知道的                                               |
| ---------- | -------------------------------------------------------- |
| 改 Prompt  | 优先改 `strategy/prompts/v2/`，不要改 Python 常量        |
| 赛后产物   | 看 `runs/<id>/role_skills.json`、`prompt_proposals.json` |
| 评测 LLM   | 只走 AgentScope；见 OpenAI 接口分析 doc                  |
| Skill 筛选 | Phase2 才按 benefit 阈值筛；当前只按**生成规则**         |

---

## 架构重构与配套修复（2026-05-24）

### 六模块迁移（`10edf0a` → `d039034`）

- **`core/` → `game_runtime/`**：规则、引擎、角色、配置、序列化
- **`adapter/` / `agents/` / `integration/` → `agent_team/`**：AgentScope、Hub、Bridge、消息隔离
- **新增 `strategy/`**：`decisions`、`phase_outputs`、`vote_intention`、`role_prompts`
- **入口 → `interface/`**：cli、tui、eval_cli、bootstrap、modes
- **删除**全部旧路径兼容 shim；非 demo/human 玩家统一 AgentScope
- **测试**：目录镜像新结构，**287 passed**

### 仓库清理（`3cf3eca`）

- 历史文档移入 `docs/archive/`；活跃文档保留根目录 4 份 + `prompt_tuning.md`
- 删除 docker/scripts/mkdocs 等非运行时目录

### 本次配套修复（审查遗漏补做）

- **恢复 `.env.example`**，README 增加 env 配置步骤
- **README** 死链改指向 `docs/archive/`；团队分工改为 `agent_team`
- **恢复 `.pre-commit-config.yaml`**，CI code-quality-check 可再次运行
- **移除 `pyproject.toml` 失效的 `poe docs_*` 任务**
- **`game_runtime/prompts/`** 删除重复 `game.py`；`GamePrompts`/`PlanStrategies` 仅以 `strategy/role_prompts.py` 为准
- **更新** `吕祎晗-发现问题.md` 问题 7 为已完成六模块方案

---

## 改了什么（按模块）

### 夜晚 / 狼队

- **梦魇狼顺序**：先跑前置阶段（含梦魇封锁），再狼队讨论 → 投票 → 结算 → 后置技能。
- **狼队夜聊**：去掉误挂的「白天放逐意向」采集，避免多余 LLM 调用和模型困惑。
- **狼票**：空票写日志；狼人投票校验「不能刀队友 / 不能刀自己」。

### 角色技能 / Action

- **白狼王**：阵营比较 + 角色名与 `RoleConfig.name` 对齐（`"White Wolf"`），技能可正常 `validate`。
- **隐狼 / 血月**：预言家查验显示阵营逻辑统一（隐狼显示好人）。
- **守墓狼 / 狼票 / 白天投票**：阵营与目标校验收紧。
- **行动处理**：`validate` 失败记 `action_rejected`；补梦魇狼、守墓狼、乌鸦、守墓人等事件日志；执行异常有日志。

### 狼美人 / 骑士 / 猎人技能接入（2026-05-24）

- **狼美人魅惑持久化**：`GameState.wolf_beauty_charmed` 字段 + 快照读写
- **骑士决斗接入**：`VotingPhaseMixin._handle_knight_duel()`
- **猎人技能确认**：毒杀跳过开枪，逻辑已正确

### 警长选举 & 白天投票

- **平票**：第一次 PK 再投；第二次警徽流失 / 无人出局
- **白天投票**：`asyncio.gather` 并发收票

### 死亡 / 殉情 / 遗言

- 殉情按昼夜写入 `night_deaths` / `day_deaths`

### 工程与杂项

- 新增 **`game_runtime/roles/names.py`**
- 阵营 API 统一为 `Camp` 枚举（2026-05-24 批量修复）

### evaluation 模块（2026-05-24 审查 + 2026-05-25 扩展）

- 2026-05-24：`event.phase` 序列化、Demo 默认语言 `zh-CN` 等
- **2026-05-25**：PostGame v2 全链路（见上文专节）

### game_runtime 代码审查（2026-05-24 第二轮）

- `vote_tie_count` 跨天重置、首夜 `round_number`、`load_game` 清空 EventLogger 等

---

## 还没动 / 已知遗留

| 项                                | 说明                                   |
| --------------------------------- | -------------------------------------- |
| 盗贼 / 魔术师                     | 夜间 planner 未实现（见「发现问题」5） |
| Skill benefit 筛选                | Phase2；当前仅生成规则                 |
| LLM Skill 提取                    | Phase2；当前规则 + 模板卡片            |
| `GamePrompts` 外置                | M2 `v2.phase.*`                        |
| PostGame `werewolf-post-game` CLI | 草案未实现                             |

---

## 夜晚流程（当前）

```
前置（梦魇/守卫/…）→ 狼队讨论 → 狼队投票 → 刀口结算 → 后置（女巫/预言家/…）→ 死亡结算
```

---

*最后更新：2026-05-25（Evaluation v2 Phase1 + Prompt 变量化 + roster 修复）*
