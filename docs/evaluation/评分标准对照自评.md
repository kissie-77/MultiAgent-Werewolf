# 评分标准对照自评

评估范围：除前端 UI 以外的 Agent、对局引擎、记忆、Skill、评测复盘、自进化闭环。

## 当前判断

当前项目非前端部分约为 **86-88/100**。建议主打进阶方向 **B：评测 + 复盘**，自进化方向 **C** 作为亮点展示。

## 对照评分项

| 评分项 | 当前状态 | 主要证据 | 剩余缺口 |
| --- | --- | --- | --- |
| 单 Agent 能力 | 基本完成 | 角色 Agent、角色 Prompt、Memory、Skill 注入链路已具备 | 需要更多真实对局证明不同角色策略质量 |
| 多 Agent 协作 | 完成度高 | `game_runtime` 负责回合、夜间行动、发言、投票、胜负流转 | 建议保留固定场景验证报告 |
| 信息隔离 | 有 checker 和测试 | `InformationIsolationChecker` 会检查私有事件是否泄漏到无权限玩家观察中 | 需要把真实评测产物汇总成 0 泄漏报告 |
| 可观测性 | 完成度高 | 事件、快照、错误、checks、PostGame、Leaderboard 均有产物 | 答辩时应展示一条从对局到复盘到策略更新的完整证据链 |
| 评测 + 复盘 | 接近满分档 | PostGame、Coach、评分、Skill 生成、Prompt proposal、Leaderboard、A/B 报告已具备 | 反事实推演还可以继续增强 |
| 自进化 Agent | 工程闭环已具备 | evolution runner、active skill 继承、prompt evolver、A/B 显著性字段、版本 manifest 已具备 | 还需要真实 20 局以上初始版 vs 终局版显著提升证据 |
| 版本回溯 | 具备候选能力 | `version_manifest.json` 保存 prompt、active skills、memory 参数、模型配置 | 可以继续补一键恢复命令 |

## 新增证据包命令

可以用下面命令生成评分证据包：

```powershell
uv run python -m llm_werewolf.interface.cli.evidence `
  --eval_root artifacts/eval_runs `
  --evolution_root artifacts/eval_runs/evolution `
  --output_dir artifacts/eval_runs/grading_evidence
```

输出：

- `grading_evidence_pack.json`
- `grading_evidence_pack.md`

这份证据包会汇总信息隔离检查、自进化轮次、A/B 显著性、版本 manifest 和剩余缺口。

## 冲满分下一步

- 跑固定 scenario 的初始版 vs 终局版 20 局以上对比。
- 用 A/B 报告保留 p-value、Wilson CI、significant 字段。
- 保留 `InformationIsolationChecker` 真实 0 泄漏证据。
- 补强反事实推演报告，说明关键投票或夜间行动如果改变会造成什么结果。

## 已补强项目

- Few-shot / 思维链：`strategy/prompts/v2/text/agent_base.md` 已加入显式 few-shot 示例和“private_thought 内部推理、public_speech 只输出公开理由”的规范。
- 反事实推演：PostGame pipeline 已接入 `counterfactual_report.json` 和 `counterfactual_report.md`。
- 文档完整度：新增 `CONTRIBUTING.md`，并补充 `docs/evaluation/反事实推演说明.md`。
