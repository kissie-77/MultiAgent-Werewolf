# 工程代码质量修复总体计划

> **模块**：reports
> **状态**：active
> **最后更新**：2026-06-05
> **关联代码**：`src/llm_werewolf/agent_team/`、`src/llm_werewolf/game_runtime/`、`src/llm_werewolf/evaluation/`、`src/llm_werewolf/interface/`、`src/llm_werewolf/ui/`
> **关联测试**：`tests/agent_team/`、`tests/game_runtime/`、`tests/evaluation/`、`tests/interface/`、`tests/ui/`
> **Agent Skill**：`.agents/skills/generated/agent-team/`、`.agents/skills/generated/game-runtime/`、`.agents/skills/generated/evaluation/`、`.agents/skills/generated/interface/`、`.agents/skills/generated/ui/`

## 总体目标

按已确认的工程质量方案，修复会影响对局正确性、信息隔离、复盘判断和长期维护的问题。

执行节奏：

- 总体进度按 25% / 50% / 75% / 100% 四个检查点汇报。
- 每增加 25% 进度，运行一局 smoke 或用户指定的正式对局确认游戏流程仍可跑通。
- 每个阶段结束后更新本文档的阶段进度、完成内容、关键决策和后续注意事项。

## 阶段计划

| 阶段 | 目标进度 | 修复范围 | 验收标准 |
|---|---:|---|---|
| P0 | 25% | 女巫毒药事件语义；决策 fallback；Yes/No fallback；UI 死导出 | 关键事件字段一致；关键决策不静默随机；UI 组件包可 import；跑通一局 smoke 对局 |
| P1 | 50% | 静态质量门禁；覆盖率/CI；公开发言 fallback 安全化；夜间技能/action 日志注册表基础；正式对局日志体验修复 | ruff 可在 dev 环境运行；CI 不跳过核心质量门禁；公开 fallback 不凭空爆身份；跑通一局正式对局 |
| P2 | 75% | checker 分级；配置命名清晰化；角色技能扩展链进一步收口 | bad case 区分幻觉/格式/策略误判；配置名和实际模型一致；跑通一局 smoke 对局 |
| P3 | 100% | 长文件低风险拆分与最终回归 | 行为不变，职责更清楚；关键回归测试通过；最终跑通一局 smoke 对局 |

## 当前阶段进度

- 当前阶段：P3
- 总体进度：94%
- 状态：P1 正式对局检查点已完成；P2 已完成配置命名清晰化、bad case 基础分级、首夜无依据公开信息引用幻觉检测、角色技能扩展链收口、特殊狼人狼票调度修复、目标选择 fallback 入口收口、全仓运行级 ruff 和覆盖率门槛核验；P3 已完成 PostGame 必需步骤失败兜底、批量 skip 收口、显式 step registry 基础版、Prompt Evolver 的 history/patch 模块拆分，以及 `bridge.py` 纯解析/Prompt 构建拆分；剩余工作集中在 `agentscope_agent.py` / `information_hub.py` 后续职责拆分、全量类型/风格门禁提升，以及更细的夜间调度元数据。

## 已完成内容

- 创建工程质量修复总体计划。
- P0：拆分女巫毒药动作与毒杀死亡结算。新对局输出 `witch_poison_used` 作为女巫毒药动作事件；毒杀死亡走 `player_died`，并写入 `reason/cause=witch_poison`；旧 `witch_poisoned` 仅作为历史日志兼容读取。
- P0：正式投票不再随机 fallback，失败时优先使用 vote intention；死亡技能和夜间关键技能不再静默随机选目标。
- P0：Yes/No 结构化调用失败不再默认 `False`，改为显式错误，由调用方记录。
- P0：修复 `llm_werewolf.ui.components` 死导出，组件包可正常 import。
- P0：相关测试通过：`uv run pytest -q --no-cov tests/evaluation/test_checkers.py tests/agent_team/test_bridge_parsing.py tests/game_runtime/test_runtime_error_logging.py tests/ui/test_components_import.py tests/game_runtime/test_event_visibility.py tests/game_runtime/test_action_processor.py tests/ui/test_console_presenter.py tests/game_runtime/test_event_formatter.py`。
- P0：25% smoke 对局通过：`uv run werewolf-eval --scenario smoke_6p_basic --games 1 --timeout_seconds 20 --seed 20260604 --output_dir artifacts/runs/checkpoint-25-smoke-20260604-104602`。
- P1：公开白天发言 fallback 改为中性安全发言，不再按真实角色生成“验人/用药/守人/开枪/狼人视角”等私有身份或技能信息；狼人夜聊 fallback 仍保留单独协调用语。
- P1：夜间技能 action 日志分发从长 `isinstance` 链收口为小型注册表，新增角色技能时只需补充动作类型到日志方法的映射。
- P1：CI 质量门禁改为渐进式：pre-commit 继续执行通用卫生钩子，ruff/mypy 历史债不一次性压死；新增 ruff 关键检查，仅拦截变更 Python 文件里的语法错误和未定义名称。
- P1：`ruff` 加入 dev 依赖组，质量门禁不再依赖 docs 组安装。
- P1：局部测试通过：`uv run pytest -q --no-cov tests/agent_team/test_agentscope_agent_fallback.py tests/game_runtime/test_action_processor.py`，结果 `22 passed`。
- P1：渐进 ruff 关键检查通过：`uv run ruff check --select E9,F821 -- <changed-python-files>`。
- P1：修复正式对局入口的旧配置引用。人机/扩展局不再指向已删除的 `configs/llm-12p-doubao.yaml`，改为当前存在的 `configs/llm-12p-kimi.yaml`；API 开局模式展示同步改为 12p Kimi/VibeAPI。
- P1：入口模式回归测试通过：`uv run pytest -q --no-cov tests/interface/test_modes.py tests/interface/test_api_actions.py::test_list_start_modes`，结果 `9 passed`。
- P1：50% 正式对局检查点通过：用户本地运行人机扩展正式局，产物目录 `artifacts/runs/20260604-125820`，结果为狼人阵营获胜。
- P1：修复警长竞选展示：`sheriff_election` 阶段新增独立控制台标题，避免首夜后警长竞选看起来像仍在夜晚。
- P1：修复单候选警长日志：只有一名玩家竞选时新增“无需投票”提示，再直接当选，避免误以为漏掉警长投票环节。
- P1：修复上帝视角日志顺序：展示信念矩阵或投票意向前，先刷新已缓冲的公开发言，避免“没看到发言却出现听完发言后的矩阵”。
- P1：补强发言校验：公开发言若停在连接词、方向词或半截标点上会被判为截断，不再把“把话题往”这类半句话写入正式发言日志。
- P1：本轮日志体验回归测试通过：`uv run pytest -q --no-cov tests/ui/test_console_presenter.py tests/strategy/test_output_format.py tests/game_runtime/test_sheriff_election.py`，结果 `36 passed`。
- P1：合并回归测试通过：`uv run pytest -q --no-cov tests/ui/test_console_presenter.py tests/strategy/test_output_format.py tests/game_runtime/test_sheriff_election.py tests/interface/test_modes.py tests/interface/test_api_actions.py::test_list_start_modes tests/agent_team/test_agentscope_agent_fallback.py tests/game_runtime/test_action_processor.py`，结果 `67 passed`；渐进 ruff 关键检查无 E9/F821 报错。
- P1：结构化调用可恢复诊断降噪，`structured_response_diagnostic`、`structured_response_interrupted`、`structured_invoke_returned_none` 改为 debug 级别，减少正式对局控制台刷屏；相关回归 `31 passed`。
- P2：配置命名清晰化完成文档收尾，interface 快速入口、配置设计说明、archive 配置说明均改为当前 12 人 Kimi/VibeAPI 正式局入口，避免继续引用已删除的 `llm-12p-doubao.yaml`。
- P2：bad case 基础分级完成，`PromptBadCaseChecker` 结果新增 `bad_case_kind`、`confidence`、`confidence_score`；`prompt_proposals` 会继承 checker 置信度，相关回归 `15 passed`。
- P2：修复 `all_agent + basic` 菜单项回退到 `configs/demo-6.yaml` 的问题，改回 `configs/llm-12p-kimi.yaml`，并把 `basic` 纳入真实 LLM 配置回归；相关回归 `12 passed`。
- P2：角色技能扩展链基础收口完成，白狼王、狼美人、梦魇狼、守护狼、乌鸦夜间技能改为 typed event，并补齐可见性、展示、checker 与回归测试；相关回归 `64 passed`。
- P2：特殊狼人夜间调度修复完成，狼票阶段统一走 `dispatch_werewolf_vote_plan`，梦魇狼/守护狼正常参与刀票，白狼王/狼美人额外技能单独收集；相关回归 `67 passed`。
- P2：首夜无依据公开信息引用幻觉检测完成，夜聊在首个公开上下文出现前若凭空引用白天发言、票型、活跃度或站边依据，会被标记为 `hallucination`；赛后 `prompt_proposals` 会生成对应 `night_strategy` 修正规则；相关回归 `20 passed`。
- P2：目标选择 fallback 策略入口收口完成，`WerewolfAdapterBridge` 与旧 `ActionSelector` 均通过 `select_target_fallback` 处理随机兜底，关键路径继续可禁用随机；相关回归 `25 passed`，架构边界 `3 passed`。
- P2：全仓运行级静态门禁核验完成，`uv run ruff check --select E9,F821 -- src tests` 无报错；全量风格规则/ty 类型债后续分批提升。
- P2：覆盖率门槛核验完成，`uv run pytest -q --cov-fail-under=20` 结果 `878 passed`，总覆盖率 `82.72%`。
- P3 预备：PostGame 必需步骤失败兜底完成，`load_context` 失败时不再裸崩，会写出 `post_game_steps.json` 并返回结构化 `PostGameResult.error`；相关回归 `6 passed`。
- P3 预备：PostGame 批量 skip 记录收口完成，依赖失败后的多步骤跳过统一走 `skip_steps`，减少长流程重复控制流；相关回归 `7 passed`。
- P3：PostGame 显式 step registry 基础版完成，步骤 ID、依赖、必需步骤和产物声明从 `pipeline.py` 散落字符串收口到 `pipeline_registry.py`；`pipeline.py` 改为通过 registry 读取步骤产物，后续定位失败步骤和扩展产物更清楚。
- P3：Prompt Evolver 模块拆分基础版完成，历史支持计算抽到 `prompt_history.py`，角色卡补丁应用抽到 `prompt_patch.py`；`prompt_evolver.py` 从 672 行降到 402 行，并清理旧版未调用的 prompt version copy/apply 死代码。
- P3：`bridge.py` 低风险职责拆分完成第一刀，纯解析逻辑抽到 `bridge_parsing.py`，Prompt 构建抽到 `bridge_prompts.py`；`WerewolfAdapterBridge` 保留原静态 API 兼容转发，核心 Agent 请求调用链不变；相关回归 `48 passed`。

## 关键决策

- smoke 对局优先使用 demo/offline 配置，避免外部模型服务波动干扰代码验证；用户指定时可用正式对局替代检查点。
- P0 优先修复会影响对局结果或 import 崩溃的问题。
- 大文件拆分放到后段，避免在关键行为修复前引入过大结构变动。
- 女巫毒药动作事件采用新名称 `witch_poison_used`；历史 artifacts 中的 `witch_poisoned` 保留兼容读取，避免旧复盘失效。
- 全仓 `ruff check src tests` 当前仍会被历史债打爆，因此 P1 不把全量 ruff 作为硬门禁；先用 `E9,F821` 这类运行级错误做渐进拦截，后续再分批提升规则范围。
- 子代理最多同时保留两个；本轮 CI 子代理因 429 失败，相关 CI 收口已由主线本地完成。
- P2 当前不直接推进到 75%，因为 75% 检查点仍需用户用正式对局或指定 smoke 对局确认流程可跑通。
- P3 先做低风险结构整理：PostGame registry、Prompt Evolver history/patch、`bridge.py` 纯解析/Prompt 构建拆分均保持外部 API 不变；后续再拆 `agentscope_agent.py` / `information_hub.py`。
- Prompt Evolver 中旧版 `_copy_prompt_version` / `_apply_proposals` 已无调用，删除比迁移更清晰；当前版本拷贝继续使用 `role_prompt_registry.copy_role_prompt_package`。

## 后续注意事项

- 每次修改事件类型或事件字段，都需要同步可见性、复盘、checker 和测试。
- 每次调整 fallback，都需要检查是否可能改变胜负结果。
- 不纳入多人类玩家专项重构，当前项目目标仍按单人类混战处理。
- P1 的 50% 检查点已用正式对局替代 smoke 完成；后续 75%/100% 检查点仍按用户指定选择 smoke 或正式对局。
- 正式对局中仍可能出现外部模型超时或 AgentScope 中断诊断；可恢复诊断已降噪，后续若仍频繁失败再单独收口调用链超时策略。
- P3 剩余拆分应保持“行为不变、测试先行”：先拆纯工具/metadata，再处理结构化调用和记忆注入。
