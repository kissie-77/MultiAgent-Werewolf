# 工程代码质量修复记录 - 2026-06-04

> **模块**：reports
> **状态**：active
> **最后更新**：2026-06-02
> **关联代码**：`src/llm_werewolf/agent_team/`、`src/llm_werewolf/game_runtime/`、`src/llm_werewolf/interface/`、`src/llm_werewolf/strategy/`、`src/llm_werewolf/ui/`
> **关联测试**：`tests/agent_team/`、`tests/game_runtime/`、`tests/interface/`、`tests/strategy/`、`tests/ui/`
> **Agent Skill**：`.agents/skills/generated/agent-team/`、`.agents/skills/generated/game-runtime/`、`.agents/skills/generated/interface/`、`.agents/skills/generated/strategy/`、`.agents/skills/generated/ui/`

本文记录 2026-06-04 围绕工程质量、人机混战日志体验、正式对局入口和核心回归门禁完成的修复工作。总体计划见 [`工程代码质量修复总体计划.md`](./工程代码质量修复总体计划.md)。

## 一、总体进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| P0 | 已完成 | 女巫毒药事件语义、关键决策 fallback、Yes/No 失败默认值、UI 死导出 |
| P1 | 已完成到 50% 检查点 | 静态质量渐进门禁、覆盖率门槛恢复、公开发言 fallback、action 日志注册表、正式对局日志体验 |
| P2 | 已完成基础闭环 | 配置命名、bad case 分级、角色技能扩展链、目标选择 fallback、运行级质量门禁和覆盖率核验已完成 |
| P3 | 进行中 | PostGame 失败兜底、批量 skip、显式 step registry、Prompt Evolver history/patch 拆分、bridge 解析/Prompt 构建拆分已完成；剩余大文件继续分阶段拆 |

## 二、今日完成内容

### 1. P0 核心正确性修复

- 拆分女巫毒药动作与毒杀死亡结算：
  - 新对局使用 `witch_poison_used` 表示女巫用毒动作。
  - 毒杀死亡走 `player_died`，并记录 `cause/reason=witch_poison`。
  - 旧 `witch_poisoned` 保留为历史产物兼容读取。
- 收紧关键决策 fallback：
  - 正式投票失败时优先使用 vote intention，不再静默随机投票。
  - 死亡技能、夜间关键技能失败时不再悄悄随机改胜负。
- Yes/No 结构化调用失败不再统一默认 `False`，改为显式错误交由阶段逻辑处理。
- 修复 `llm_werewolf.ui.components` 导出不存在组件的问题，并补 import 回归。

### 2. P1 工程质量门禁

- `ruff` 加入 dev 依赖组。
- CI 质量检查改为渐进门禁：
  - pre-commit 保留通用卫生钩子。
  - ruff/mypy 历史债不一次性压死。
  - 新增变更 Python 文件的 `E9,F821` 关键错误检查。
- 测试 CI 的 coverage fail-under 从 0 恢复为非零门槛 `20`，后续再逐步 ratchet。

### 3. 正式对局入口与配置命名

- 修复人机/扩展局仍引用已删除 `configs/llm-12p-doubao.yaml` 的问题。
- 12 人/扩展局真实 LLM 配置改为 `configs/llm-12p-kimi.yaml`。
- API 开局模式展示同步改为 12p Kimi / VibeAPI。
- 增加内置 mode 配置路径存在性回归，避免以后入口指向不存在配置。
- 清理 interface/config 文档中的旧入口误导：
  - `docs/interface/README.md` 增加交互式入口和 18 人人机扩展局命令。
  - `docs/interface/DESIGN.md` 将当前 12 人正式局说明切到 Kimi/VibeAPI 与 `VIBE_API_KEY`。
  - `configs/archive/README.md` 明确 `llm-12p-doubao.yaml` 已退出 active entry modes。
- 修复 `all_agent + basic` 菜单项曾被 CI 修复提交改回 `configs/demo-6.yaml` 的回退问题：
  - 该入口现已改回 `configs/llm-12p-kimi.yaml`。
  - `tests/interface/test_modes.py` 将 `basic` 纳入真实 LLM / Kimi / VibeAPI 配置断言，防止再次回退为 DemoAgent。

### 4. 人机混战与日志体验修复

- `sheriff_election` 阶段新增独立控制台标题，避免首夜后警长竞选看起来仍在夜晚。
- 单候选警长新增“无需投票”日志，再直接当选，避免误以为漏掉警长投票。
- 上帝视角展示信念矩阵或投票意向前，先刷新公开发言缓冲，避免“没看到发言却出现听完发言后的矩阵”。
- 公开发言校验新增截断判断，拒绝“把话题往”这类停在连接词或方向词上的半句话。
- 将可恢复的结构化诊断日志降噪：
  - `structured_response_diagnostic`
  - `structured_response_interrupted`
  - `structured_invoke_returned_none`
  以上内容改为 debug 级别，避免正式对局控制台刷屏；真正的调用异常仍保留 warning。

### 5. 正式对局检查点

- 用户本地完成 18 人人机扩展正式局检查：
  - 产物目录：`artifacts/runs/20260604-125820`
  - 结果：狼人阵营获胜
- 该局暴露的问题已回写为 UI / strategy / sheriff 回归测试。

### 6. Bad Case 分级

- `PromptBadCaseChecker` 的 bad case 结果新增结构化分级字段：
  - `bad_case_kind`：区分 `format_error`、`low_information`、`strategy_mistake`、`hallucination`。
  - `confidence`：高/中/低置信度标签。
  - `confidence_score`：0-1 数值置信度，便于后续自动提案筛选。
- `prompt_proposals` 生成 bad case 提案时优先继承 checker 给出的 `confidence_score`，避免后续 Prompt Evolver 把所有 bad case 视为同一置信度。

### 7. 角色技能扩展链收口

- 扩展夜间技能不再落成泛型 `EventType.MESSAGE`，改为稳定 typed event：
  - `white_wolf_killed`
  - `wolf_beauty_charmed`
  - `nightmare_blocked`
  - `guardian_wolf_protected`
  - `raven_marked`
- 同步补齐链路：
  - `ActionProcessor` 输出 typed event。
  - `event_visibility` 按原规则控制可见性：狼队技能仅狼队可见，单人技能仅行动者可见。
  - `EventFormatter` / `ConsolePresenter` 支持样式和夜间行动分组展示。
  - `RoleSkillChecker` / `DecisionConsistencyChecker` 支持这些新事件的字段检查与目标一致性检查。
- 修复特殊狼人夜间调度：
  - 狼票阶段统一走 `dispatch_werewolf_vote_plan`，确保梦魇狼、守护狼等特殊狼人也会正常参与狼刀投票。
  - 白狼王、狼美人的额外技能作为狼票阶段特殊技能单独收集，避免把“狼刀投票”和“特殊技能”混在同一个 planner 里。
  - 修复后不会在狼票阶段重复触发梦魇狼封锁或守护狼保护。

### 8. 首夜无依据公开信息引用幻觉检测

- `PromptBadCaseChecker` 新增夜间发言幻觉检测：
  - 在首个公开发言/投票上下文出现前，如果夜聊凭空引用“白天发言”“票型”“投票记录”“发言活跃/站边/带队”等公开阶段事实，会标记为 `hallucination`。
  - 已避免误伤后续夜晚正常复盘：只要此前已经出现过公开发言、投票或放逐事件，夜间再引用公开阶段信息不会被该规则拦截。
- `prompt_proposals` 对该类 bad case 生成更具体的 `night_strategy` 修正规则：
  - 首夜或尚无公开发言前，夜聊只能依据座位、角色池、狼队信息和刀口收益。
  - 不得编造白天发言、票型、活跃度或站边依据。

### 9. PostGame 必需步骤失败兜底

- 修复 `run_post_game_pipeline` 的 `load_context` 失败处理：
  - 之前 `load_context` 使用 `required=True`，一旦失败会直接抛出异常，导致主流程里写 `post_game_steps.json` 和返回结构化 `PostGameResult.error` 的兜底分支走不到。
  - 现在 `load_context` 仍按必需步骤处理，但失败后会记录 failed step、写出 `post_game_steps.json`，并返回 `error="load_context failed"`。
- 这样赛后复盘即使因上下文读取异常失败，也能在产物目录里看到失败步骤和错误原因，便于定位而不是裸崩。

### 10. 决策目标 fallback 入口收口

- 新增 `game_runtime.prompts.decision_fallback.select_target_fallback`，统一目标选择失败后的 fallback 策略：
  - 禁用随机 fallback 时返回空目标，不再由调用点各自判断。
  - 启用随机 fallback 时统一记录 `reason`，便于后续日志和复盘识别“这是兜底行为”。
- `WerewolfAdapterBridge.request_seat_choice` 和旧 `ActionSelector.get_target_from_agent` 均改为调用该统一 helper。
- 正式投票等关键路径仍通过 `fallback_random=False` 禁止随机，保持 P0/P1 收紧后的行为不变。

### 11. PostGame 跳过步骤记录收口

- 新增 `pipeline_steps.skip_steps`，把“依赖失败后批量跳过后续步骤”的重复逻辑收进统一 helper。
- `run_post_game_pipeline` 中 `camp_persuasion` 失败后的评分/收益步骤跳过，以及 prompt/skill/coach 相关步骤跳过，均改为走该 helper。
- 该改动不改变 PostGame 产物语义，只降低长流程里的重复控制流。

### 12. 全仓运行级门禁与覆盖率核验

- 全仓运行级 ruff 检查已通过：`uv run ruff check --select E9,F821 -- src tests`。
  - 这代表语法错误和未定义名称不再只查变更文件，而是当前全仓可作为 CI 关键门禁运行。
  - 全量风格规则和类型债仍不一次性压死，后续按批次提升。
- 全量测试和覆盖率门槛已通过：`uv run pytest -q --cov-fail-under=20`。
  - 结果：`878 passed`。
  - 总覆盖率：`82.72%`。
  - 已高于 CI 当前 20% 门槛，也高于 `pyproject.toml` 默认 80% 目标。
- 同步修正 API 默认开局测试预期：
  - 当前默认正式入口为 `llm-12p-kimi`，不再是 `demo-6`。

### 13. 2026-06-05 续修：PostGame step registry

- 新增 `evaluation/post_game/pipeline_registry.py`，集中声明 PostGame 步骤元数据：
  - `step_id`
  - `artifacts`
  - `depends_on`
  - `required`
- `pipeline.py` 不再散落维护主要步骤产物列表，改为通过 registry 读取：
  - `load_context`
  - `episodic`
  - `vote_swing`
  - `camp_persuasion`
  - `log_views`
  - `intention_scores`
  - `score_contexts`
  - `mvp_scores`
  - `benefit_scores`
  - `llm_replay`
  - `game_quality_report`
  - `counterfactual`
  - `prompt_proposals`
  - `role_skills`
  - `coach`
- 该改动保持执行顺序和产物语义不变，只把步骤配置显式化，方便后续定位失败步骤，也为继续拆 PostGame 长流程做准备。
- 增加 registry 回归测试，确保必需步骤、依赖关系和关键产物声明不会被误改。

### 14. 2026-06-05 续修：Prompt Evolver 模块拆分

- 拆分 `evaluation/evolution/prompt_evolver.py`：
  - 历史支持计算移动到 `prompt_history.py`。
  - 角色卡补丁应用移动到 `prompt_patch.py`。
  - `evolve_prompt_from_run` 对外入口保持不变。
- 清理旧版未调用代码：
  - 删除 `_copy_prompt_version`。
  - 删除 `_apply_proposals`。
  - 删除旧版 target spec 解析和 text file append 辅助函数。
- 当前版本拷贝逻辑继续使用已有成熟入口 `role_prompt_registry.copy_role_prompt_package`，不再保留另一套未调用 copy 实现。
- 拆分后 `prompt_evolver.py` 从 672 行降到 402 行，职责更集中：主文件负责筛选提案、编排演进、写结果；history/patch 逻辑由独立模块承接。

### 15. 2026-06-05 续修：bridge 解析与 Prompt 构建拆分

- 新增 `agent_team/bridge_parsing.py`：
  - `parse_target_selection`
  - `parse_yes_no`
  - `parse_multi_target_selection`
  - `parse_speech`
- 新增 `agent_team/bridge_prompts.py`：
  - `build_target_selection_prompt`
  - `build_yes_no_prompt`
  - `build_multi_target_prompt`
  - `build_speech_prompt`
  - `build_witch_night_prompt`
  - `build_vote_intention_prompt`
  - `build_mind_state_prompt`
- `WerewolfAdapterBridge` 保留原静态方法签名，只改为兼容转发，避免影响现有调用点。
- 该拆分只迁移纯解析和纯 Prompt 构建逻辑，不动 Agent 请求、结构化调用、metadata、fallback 和记忆链路。
- `bridge.py` 从 966 行降到 811 行，后续可继续拆结构化 requester、metadata/fallback 和 AgentScope 响应处理。

## 三、验证记录

已通过以下验证：

```powershell
uv run pytest -q --no-cov tests/ui/test_console_presenter.py tests/strategy/test_output_format.py tests/game_runtime/test_sheriff_election.py
```

结果：`36 passed`

```powershell
uv run pytest -q --no-cov tests/ui/test_console_presenter.py tests/strategy/test_output_format.py tests/game_runtime/test_sheriff_election.py tests/interface/test_modes.py tests/interface/test_api_actions.py::test_list_start_modes tests/agent_team/test_agentscope_agent_fallback.py tests/game_runtime/test_action_processor.py
```

结果：`67 passed`

```powershell
$files = git diff --name-only --diff-filter=ACMR -- '*.py'
if ($files) { uv run ruff check --select E9,F821 -- $files }
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/agent_team/test_structured_invoke.py tests/agent_team/test_agentscope_agent_fallback.py tests/agent_team/test_agentscope_agent_config.py
```

结果：`31 passed`

```powershell
uv run pytest -q --no-cov tests/evaluation/test_checkers.py tests/evaluation/test_post_game_pipeline.py::test_bad_case_proposal_uses_role_stage_target_when_player_is_known
```

结果：`15 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/evaluation/core/checkers.py src/llm_werewolf/evaluation/post_game/prompt_proposal.py tests/evaluation/test_checkers.py tests/evaluation/test_post_game_pipeline.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/evaluation/post_game/test_pipeline_steps.py tests/evaluation/test_post_game_pipeline.py
```

结果：`6 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/evaluation/post_game/pipeline.py tests/evaluation/post_game/test_pipeline_steps.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/game_runtime/test_decision_fallback.py tests/game_runtime/test_action_selector.py tests/agent_team/test_bridge_parsing.py tests/game_runtime/test_runtime_error_logging.py
```

结果：`25 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/game_runtime/prompts/decision_fallback.py src/llm_werewolf/agent_team/bridge.py src/llm_werewolf/game_runtime/prompts/selector.py tests/game_runtime/test_decision_fallback.py tests/game_runtime/test_action_selector.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/architecture/test_import_boundaries.py
```

结果：`3 passed`

```powershell
uv run pytest -q --no-cov tests/evaluation/post_game/test_pipeline_steps.py tests/evaluation/test_post_game_pipeline.py
```

结果：`7 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/evaluation/post_game/pipeline.py src/llm_werewolf/evaluation/post_game/pipeline_steps.py tests/evaluation/post_game/test_pipeline_steps.py
```

结果：无 E9/F821 报错。

```powershell
uv run ruff check --select E9,F821 -- src tests
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/agent_team/test_bridge.py tests/agent_team/test_bridge_parsing.py tests/agent_team/test_demo_agent.py tests/strategy/test_output_format.py tests/game_runtime/test_runtime_error_logging.py tests/game_runtime/test_decision_fallback.py tests/game_runtime/test_action_selector.py
```

结果：`48 passed`

```powershell
uv run ruff check --select E9,F401,F821 -- src/llm_werewolf/agent_team/bridge.py src/llm_werewolf/agent_team/bridge_parsing.py src/llm_werewolf/agent_team/bridge_prompts.py tests/agent_team/test_bridge.py tests/agent_team/test_bridge_parsing.py tests/agent_team/test_demo_agent.py tests/strategy/test_output_format.py
```

结果：无 E9/F401/F821 报错。

```powershell
uv run pytest -q --no-cov tests/interface/test_api_actions.py::test_start_game_default_mode tests/interface/test_modes.py tests/interface/test_api_actions.py::test_list_start_modes
```

结果：`10 passed`

```powershell
uv run pytest -q --cov-fail-under=20
```

结果：`878 passed`，总覆盖率 `82.72%`。

```powershell
uv run pytest -q --no-cov tests/interface/test_modes.py tests/interface/test_startup_menu.py tests/interface/test_api_actions.py::test_start_game_mode_badge_flow
```

结果：`12 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/interface/cli/runtime/modes.py tests/interface/test_modes.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/game_runtime/test_action_processor.py tests/game_runtime/test_event_visibility.py tests/evaluation/test_checkers.py tests/ui/test_console_presenter.py
```

结果：`64 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/game_runtime/types/enums.py src/llm_werewolf/game_runtime/events/event_visibility.py src/llm_werewolf/game_runtime/events/event_formatter.py src/llm_werewolf/game_runtime/engine/action_processor.py src/llm_werewolf/ui/console_presenter.py src/llm_werewolf/evaluation/core/checkers.py tests/game_runtime/test_action_processor.py tests/game_runtime/test_event_visibility.py tests/evaluation/test_checkers.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/game_runtime/test_night_scheduler.py tests/game_runtime/test_action_processor.py tests/game_runtime/test_event_visibility.py tests/evaluation/test_checkers.py tests/ui/test_console_presenter.py
```

结果：`67 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/game_runtime/night_scheduler.py src/llm_werewolf/game_runtime/registries/role_night_plans.py tests/game_runtime/test_night_scheduler.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/evaluation/test_post_game_pipeline.py tests/evaluation/test_checkers.py
```

结果：`20 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/evaluation/core/checkers.py src/llm_werewolf/evaluation/post_game/prompt_proposal.py tests/evaluation/test_checkers.py tests/evaluation/test_post_game_pipeline.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/evaluation/post_game/test_pipeline_steps.py tests/evaluation/test_post_game_pipeline.py
```

结果：`8 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/evaluation/post_game/pipeline.py src/llm_werewolf/evaluation/post_game/pipeline_registry.py src/llm_werewolf/evaluation/post_game/pipeline_steps.py tests/evaluation/post_game/test_pipeline_steps.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/evaluation/test_prompt_evolver.py tests/evaluation/test_evolution_runner.py tests/interface/test_finalize_run.py
```

结果：`29 passed`

```powershell
uv run ruff check --select E9,F821 -- src/llm_werewolf/evaluation/evolution/prompt_evolver.py src/llm_werewolf/evaluation/evolution/prompt_history.py src/llm_werewolf/evaluation/evolution/prompt_patch.py tests/evaluation/test_prompt_evolver.py tests/evaluation/test_evolution_runner.py tests/interface/test_finalize_run.py
```

结果：无 E9/F821 报错。

```powershell
uv run pytest -q --no-cov tests/game_runtime/test_decision_fallback.py tests/game_runtime/test_action_selector.py tests/agent_team/test_bridge_parsing.py tests/game_runtime/test_runtime_error_logging.py tests/evaluation/post_game/test_pipeline_steps.py tests/evaluation/test_post_game_pipeline.py tests/evaluation/test_prompt_evolver.py tests/evaluation/test_evolution_runner.py tests/interface/test_finalize_run.py tests/interface/test_api_actions.py::test_start_game_default_mode tests/interface/test_modes.py tests/interface/test_api_actions.py::test_list_start_modes
```

结果：`72 passed`

```powershell
uv run ruff check --select E9,F821 -- src tests
```

结果：无 E9/F821 报错。

## 三、2026-06-02 后端功能批次

本轮对齐代码与模块三件套文档（不含 `ui/`、`frontend/`）。详细设计见各模块 `DESIGN.md` / `ROADMAP.md`。

| 主题 | 模块 | 摘要 |
|------|------|------|
| 信念 Skill 注入 | agent_team | `InformationHub._refresh_actor_belief_skills`：私密 LLM 决策前统一 refresh（选座、女巫、投票意向等） |
| Prompt 外置与清理 | strategy | `GamePrompts`/`PlanStrategies` → `phase/`、`plans/`；删除 legacy `prompts/v2/`；流程文案 Schema 化 |
| 结构化输出 | agent_team + strategy | phase prompt 以 `SeatChoiceDecision` 等描述；Bridge 保留 `[[n]]` 回退 |
| 规则与装配 | game_runtime | 白狼白天 `self_explode` 跳过投票进黑夜；`setup_game` 时 hub 未注入 fail-fast |
| Bridge fallback | agent_team | yes/no 失败 → False；vote_intention 失败 → seat=0 |
| 记忆边界 | agent_team | 语义提取迁至 `semantic_extraction.py`；局末不再 lazy import coach |
| Coach / 扫描 | evaluation | 同级 run 目录 snapshot；`scan_run_dir` 固定跑 `PromptBadCaseChecker` |
| 可观测 | observability + interface | `/ready` 多 key 检查、503 + checks 明细；CLI 失败写 `run_meta` |

测试：`tests/agent_team/test_information_hub_belief_skills_refresh.py`；strategy 套件 59 passed（`pytest -o addopts=`）。

## 四、剩余任务状态

| 原任务 | 当前状态 | 备注 |
|--------|----------|------|
| 统一决策 fallback 策略入口 | 已完成基础版 | 目标选择失败策略已统一到 `select_target_fallback`；关键路径仍可禁用随机 |
| 静态质量门禁 | 已完成运行级门禁 | 全仓 E9/F821 通过；全量风格规则/ty 类型债后续分批提升 |
| 覆盖率约束 | 已完成 CI 门槛核验 | 全量测试 `878 passed`，覆盖率 `82.72%`；人机关键链路覆盖后续可继续增强 |
| 夜间调度与行动日志注册表 | 已完成基础版 | action 日志注册表、扩展技能 typed event、特殊狼人狼票调度已做；后续只剩更细的 wake_order 元数据设计 |
| 配置命名清晰化 | 已完成本轮收尾 | 代码入口、API 展示、interface 文档和 archive 说明均已指向 Kimi/VibeAPI 12 人配置 |
| bad case 分级 | 已完成基础版并补强一类幻觉 | checker 已输出分类与置信度；已补“首夜凭空引用白天信息”检测，后续可继续扩展更多幻觉模式 |
| 角色技能扩展链收口 | 已完成基础版 | 扩展夜间技能已补 typed event、可见性、展示、checker 回归 |
| 大文件职责拆分 | 部分完成 | `bridge.py` 已完成纯解析/Prompt 构建第一刀拆分；`agentscope_agent.py`、`information_hub.py` 后续分阶段拆 |
| PostGame / Prompt Evolver 长流程整理 | 基础版已完成 | 已修复 `load_context` 必需步骤失败裸崩、收口批量 skip 记录、新增显式 step registry；Prompt Evolver 已拆 history/patch 模块并删除旧版未调用 copy/apply 死代码 |

## 五、后续建议

1. 跑一局正式对局作为 P2 的 75% 检查点。
2. 后续如果正式局继续出现异常复盘，可继续扩展更多幻觉类 bad case 模式。
3. 进入 P3：拆大文件和 PostGame 长流程，避免在行为仍不稳定时做大重构。
