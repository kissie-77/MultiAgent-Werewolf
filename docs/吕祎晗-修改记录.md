# 吕祎晗 - 修改记录

> 给同事看的变更摘要。详细待办见 [吕祎晗-发现问题.md](./吕祎晗-发现问题.md)。  
> 最近推送：`72eb96c`（`main`，2026-05-24）  
> 最近更新：2026-05-24（core 审查修复 7/8 + 狼美人/骑士接入）

---

## 改了什么（按模块）

### 夜晚 / 狼队
- **梦魇狼顺序**：先跑前置阶段（含梦魇封锁），再狼队讨论 → 投票 → 结算 → 后置技能。
- **狼队夜聊**：去掉误挂的「白天放逐意向」采集，避免多余 LLM 调用和模型困惑。
- **狼票**：空票写日志；狼人投票校验「不能刀队友 / 不能刀自己」。

### 角色技能 / Action
- **白狼王**：阵营比较 + 角色名与 `RoleConfig.name` 对齐（`"White Wolf"`），技能可正常 `validate`。
- **隐狼 / 血月**：预言家查验显示阵营逻辑统一（隐狼显示好人）。
- **守墓狼 / 狼票 / 白天投票**：阵营与目标校验收紧。
- **行动处理**：`validate` 失败记 `action_rejected`；补梦魇狼、守墓狼、乌鸦、守墓人等事件日志；执行异常有日志。

### 狼美人 / 骑士 / 猎人技能接入（2026-05-24）
- **狼美人魅惑持久化**：`GameState.wolf_beauty_charmed` 字段 + 快照读写；`WolfBeautyCharmAction.execute()` 写入；死亡处理优先读 `game_state`
- **骑士决斗接入**：`VotingPhaseMixin._handle_knight_duel()` 在投票前执行，通过 LLM 选目标 → `KnightDuelAction` 执行 → 狼死/骑士死 → 处理情侣/狼美人连锁死亡；三个语言区域添加 `knight_duel_*` 消息模板
- **猎人技能确认**：毒杀（`death_cause == "witch_poison"`）跳过开枪，狼刀/投票正常触发，逻辑已正确无需改动

### 警长选举 & 白天投票
- **平票**：第一次 PK 再投；第二次警徽流失 / 无人出局（警长、放逐各一套）。
- **异常**：参选、投票阶段 LLM 失败不再静默，有 error 日志。
- **文案**：硬编码中英文改为 `locale.get()`。
- **白天投票**：`asyncio.gather` 并发收票。

### 死亡 / 殉情 / 遗言
- 殉情按昼夜写入 `night_deaths` / `day_deaths`。
- 猎人开枪失败有 fallback + 日志；警徽移交、死亡技能提示走 locale。

### 工程与杂项
- 新增 **`core/roles/names.py`**：统一 `RoleNames` 与 `seer_apparent_camp()` 等，避免 `"WhiteWolf"` / `"Hidden Wolf"` 混用。
- 移除模块级重复 `dotenv.load_dotenv()`；prompt 解析小修（`re` 导入、`parse_yes_no`、多目标括号解析）。
- `game_config` 校验参数类型标注修正。
- **未做**：JSON Schema 约束 LLM 输出（试过，已回滚，仍用 `[[n]]` + AgentScope 结构化主路径）。

### roles 模块审查与修复（2026-05-24）
- **`__init__.py` 导出符号缺失**：补充 `VictoryGoal`、`ROLE_CATALOG`、`get_catalog`、`get_definition`、`RoleDefinition` 的导入，修复 `from llm_werewolf.core.roles import ROLE_CATALOG` 报错
- **`names.py` 阵营比较错误**：`player_camp_is` 中 `player.get_camp() == camp.value` 改为 `player.get_camp() == camp`（直接比较枚举）；`seer_apparent_camp` 中冗余 `Camp()` 包装改为条件判断
- **角色 description 硬编码英文**：将所有角色（狼人阵营 8 个 + 好人阵营 12 个 + 中立阵营 3 个）的 `description` 从英文改为中文，包括 `private_notes` 中的英文提示
- **`build_werewolf_team_context` 硬编码英文**：改为中文提示词

### types 模块审查与修复（2026-05-24）
- **`__init__.py` 导出符号缺失**：补充 `VictoryGoal` 的导入，修复 `from llm_werewolf.core.types import VictoryGoal` 报错
- **`Event.phase` 类型不一致**：`phase: str` 改为 `phase: GamePhase`，同步更新 `EventLogger.create_event` 签名
- **`PlayerProtocol.get_camp()` 返回类型错误**：从 `str` 改为 `Camp`，与实际实现一致
- **移除废弃的 `RoleProtocol.night_action`**：该方法已标记弃用且无调用方，从协议中移除
- **`ActionProtocol.execute()` 返回类型不精确**：从 `list[str]` 改为 `list[str] | None`，反映实际可能返回 `None` 的情况

### evaluation 模块审查与修复（2026-05-24）
- **`event.phase` 枚举序列化问题**：`checkers.py` 和 `runner.py` 中多处 `event.phase` 直接赋值到 data dict，改为 `event.phase.value` 确保 JSON 序列化一致
- **`InformationIsolationChecker._collect_sensitive_fragments` 重复提取**：移除重复的 `result = data.get("result")` 提取，因为 `_SENSITIVE_DATA_KEYS` 已包含 `"result"`
- **`runner.py` DemoAgent 导入路径错误**：从 `llm_werewolf.core.agent` 改为 `llm_werewolf.agents.base`
- **`EvaluationScenario.language` 默认值**：从 `"en-US"` 改为 `"zh-CN"`，与项目中文定位一致
- **`AsyncFlowChecker` 阶段比较**：`previous.phase` 和 `current.phase` 改为 `.value` 提取字符串后再比较

### 阵营 API 全面统一（2026-05-24）
- **`Player.get_camp()` 返回类型统一**：从 `str`（`self.role.camp.value`）改为 `Camp` 枚举（`self.role.camp`），同步更新 `core/player.py` 导入 `Camp`
- **`GameState.get_players_by_camp()` 参数统一**：从 `camp: str` 改为 `camp: Camp`，比较逻辑从 `p.get_camp() == camp` 变为枚举直接比较
- **`GameState.count_alive_by_camp()` 参数统一**：从 `camp: str` 改为 `camp: Camp`
- **`GameStateProtocol` 协议补全**：新增 `get_players_by_camp(self, camp: Camp) -> list[PlayerProtocol]` 签名，`count_alive_by_camp` 签名已正确
- **`checkers.py` 类型同步**：`_check_harmful_power_targets` 的 `player_camps` 参数从 `dict[str, str]` 改为 `dict[str, Camp]`，字符串比较 `"villager"` 改为 `Camp.VILLAGER`
- **`names.py` fallback 简化**：`seer_apparent_camp()` 中 `Camp(target.get_camp()) if isinstance(target.get_camp(), str) else target.get_camp()` 简化为 `target.get_camp()`（不再需要类型兼容）
- **字符串拼接处适配**：`villager.py` 守墓人查验和 `action_processor.py` locale 格式化中，`get_camp()` 改为 `get_camp().value` 以获取字符串值
- **调用方已正确使用枚举**：`night_phase.py`、`role_night_plans.py`、`message_router.py`、`visibility.py`、`death_handler.py` 等 8 处已使用 `Camp.WEREWOLF` / `Camp.VILLAGER` 比较，无需修改

### 修复 8：问题 8.3–8.7、8.9 批量修复（2026-05-24）
- **8.3 放逐 PK**：`voting_phase._conduct_pk_speeches` 仅 PK 位发言；新增 `EngineContexts.exile_pk_speech`
- **8.4 狼票分离**：`plan_nightmare_wolf` / `plan_guardian_wolf` 移除前置 `_plan_werewolf_pack_vote`
- **8.5 血月使徒**：`participates_in_wolf_team()` 过滤未变身；`offer_blood_moon_transform()` LLM 确认变身；狼队 `visible_to` 同步过滤
- **8.6 特殊胜负**：`check_special_victory()` 接入主链；白狼情侣 `white_lover_wolf`；盗贼 `has_chosen` 后随阵营
- **8.7 读档**：抽取 `_wire_game_state()`；`load_game` 重绑 Hub + re-attach agents
- **8.9 守墓人**：`graveyard_checked` 字段 + 快照 + planner 历史上下文
- **测试**：`test_victory` 更新；新增 `test_graveyard_and_wolf_team.py`

### 修复 7：`enable_sheriff` 接入 + 刀口仅女巫可见（2026-05-24）
- **8.1 警长开关**：`GameConfig.enable_sheriff` 写入 `GameState`；`False` 时开局标记 `sheriff_election_done=True`，`next_phase` / `play_game` 仅在 `enable_sheriff=True` 时进入 `SHERIFF_ELECTION`；快照读写 `enable_sheriff`
- **8.2 刀口可见性**：`WEREWOLF_KILLED` 加入 `WITCH_ONLY_TYPES`；`_log_event` 传入存活女巫 ID，事件仍进全量日志，但 `get_events_for_player` 仅女巫可见
- **测试**：`test_game_state` 补开关用例；新增 `test_event_visibility.py`

### core 代码审查与修复（2026-05-24，第二轮）
- **`vote_tie_count` 跨天未重置**：进入 `DAY_VOTING` 时在 `next_phase()` 与 `run_voting_phase()` 重置为 0，避免第二天首次平票被误判为「第二次平票」
- **首夜 `round_number` 为 0**：`set_phase(NIGHT)` 时若 `round_number == 0` 则置 1（`play_game` 不经 `next_phase(SETUP)` 直接开夜）
- **血月使徒 `winner_ids` 不一致**：狼人parity胜时 `winner_ids` 与计数同样排除未变身血月使徒
- **`load_project_dotenv` 缓存返回值**：二次调用返回首次实际加载路径，不再误报不存在的 `.env`
- **`load_config` 空 YAML**：非 dict 时抛 `ValueError`，避免 `TypeError`
- **`get_vote_counts` 脏票**：跳过已死亡 / 无投票权玩家的 `votes` 条目
- **`Player.revive()` 恢复投票权**：清除 `NO_VOTE` 并恢复 `can_vote_flag`
- **`Role.can_act_today` 尊重 `disabled`**：与 `can_act_tonight` 一致（长老惩罚后）
- **预言家私密笔记**：`get_private_notes` 改用 `seer_apparent_camp()`，与查验结果一致
- **女巫 `witch_*_used` 标志**：`WitchSaveAction` / `WitchPoisonAction.execute` 写入 `game_state`
- **狼队讨论视角**：`build_shared_observation` 以当前发言狼人为 `players[0]`
- **长老遇刀存活**：改记 `MESSAGE` 而非 `PLAYER_DIED`；女巫救/守卫挡刀结算处去掉重复事件（action 阶段已记）
- **`load_game` 清空 EventLogger**：避免读档后混入上一局事件
- **`EventFormatter`**：补 `VOTE_INTENTION_SNAPSHOT` 样式
- **测试**：`test_events` / `test_event_formatter` 中非法 `phase` 改为合法 `GamePhase` 值（当前 core 测试 169 项全过）

---

## 还没动 / 已知遗留

| 项 | 说明 |
|----|------|
| 盗贼 / 魔术师 | 夜间 planner 未实现，调度后静默跳过（见「发现问题」5） |
| 板子平衡 / 超时档位 / PlayerConfig 文档 | 见「发现问题」2–4 |

---

## 夜晚流程（当前）

```
前置（梦魇/守卫/…）→ 狼队讨论 → 狼队投票 → 刀口结算 → 后置（女巫/预言家/…）→ 死亡结算
```

---

*最后更新：2026-05-24*
