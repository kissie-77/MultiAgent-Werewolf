# 吕祎晗 - 发现问题

## 问题 1：WolfBeauty 魅惑状态未持久化
- **文件**：`src/llm_werewolf/core/actions/werewolf.py`、`game_state.py`、`serialization.py`
- **修复**：`wolf_beauty_charmed` 写入 `game_state` 并纳入快照；死亡处理优先读 `game_state`
- **状态**：✅ 已修复（2026-05-24）

## 问题 2：角色分配在边界人数时可能不平衡
- **文件**：`src/llm_werewolf/core/config/presets.py`
- **问题**：`_allocate_werewolf_roles` 和 `_allocate_villager_roles` 在某些边界人数时角色分配可能不平衡
  - 例如 11 人时：狼人 3 个（Werewolf×2 + AlphaWolf）+ 好人 5 个（Seer + Witch + Guard + Hunter + Cupid）+ 平民 3 个 = 11
  - 11 人刚好触发 `Cupid` 和 `AlphaWolf` 同时加入，好人阵营偏强
- **建议**：审查各人数段的狼好比例，调整角色分配逻辑
- **状态**：⏸️ 待处理

## 问题 3：超时档位在 13 人时跨度过大
- **文件**：`src/llm_werewolf/core/config/presets.py`
- **问题**：`_get_timeouts` 中 12 人返回 (60, 300, 60)，13 人直接跳到 (90, 400, 90)，跨度较大
- **建议**：增加 13-16 人的中间档超时设置
- **状态**：⏸️ 待处理

## 问题 4：PlayerConfig 的 model 字段描述与实际行为不一致
- **文件**：`src/llm_werewolf/core/config/player_config.py`
- **问题**：文档描述说 `model=<model_name> + base_url` 使用 LLM，但实际是通过 `create_agent()` 的 `use_agentscope` 参数决定走哪个后端，`model` 字段本身只用于区分 `human`/`demo`
- **建议**：更新 docstring 描述，准确反映实际行为
- **状态**：⏸️ 待处理

## 问题 5：盗贼 / 魔术师夜间行动未实现
- **文件**：
  - `src/llm_werewolf/core/role_night_plans.py`（`NIGHT_PLANNERS` 无对应条目）
  - `src/llm_werewolf/core/night_scheduler.py`（`Thief` 在 `PRE_WOLF_ROLE_NAMES` 中调度）
  - `src/llm_werewolf/core/roles/neutral.py`（`Thief`）
  - `src/llm_werewolf/core/roles/villager.py`（`Magician`）
- **问题**：
  - **盗贼**：首夜被 `NightSkillScheduler` 调度，但 `dispatch_night_plan` 找不到 planner 时静默返回 `[]`，玩家无法选身份
  - **魔术师**：在女巫之后被 catch-all 调度，同样无 planner，交换身份技能不生效
  - 两者涉及「身份牌池 / 角色实例替换 / 阵营变更」等复杂状态，不是简单补一个 LLM 选目标能解决的
- **建议**（暂不实现）：
  1. 盗贼：设计 `available_roles` 发牌逻辑 + 选牌后替换 `Player.role` 并更新阵营
  2. 魔术师：设计 `MagicianSwapAction` + 双目标选座 + 交换两名玩家的 `role` 实例
  3. 板子配置层决定是否启用这两个角色，避免 half-implemented 进随机局
- **状态**：⏸️ 待处理（复杂度高，本轮跳过）

## 问题 6：事件日志双轨（EventLogger vs GameState.event_history）
- **文件**：
  - `src/llm_werewolf/core/events.py`（`EventLogger`）
  - `src/llm_werewolf/core/game_state.py`（`event_history` / `record_event`）
  - `src/llm_werewolf/core/engine/base.py`（引擎 `_log_event` → `EventLogger`）
- **现状**：
  | 路径 | 写入方 | 读取方 | 是否活跃 |
  |------|--------|--------|----------|
  | `EventLogger.events` | 引擎 `_log_event`、各 phase handler | 玩家 observation 构建、evaluation runner | ✅ 活跃 |
  | `GameState.event_history` | `record_event()` | `get_recent_events()` | ❌ 无调用方 |
- **可行性分析**（保留 `event_history` 供后续日志 / 存档）：
  1. **短期（低改动）**：在 `GameEngine._log_event` 创建事件后追加 `game_state.record_event(event)`，双写保持同步；evaluation 仍读 `EventLogger`
  2. **中期（存档）**：在 `GameStateSnapshot` 增加可选 `events: list[EventSnapshot]`，save/load 时序列化 `EventLogger.events` 或 `event_history`（需 JSON 化 `Event`，`phase`/`event_type` 用 `.value`）
  3. **长期（统一）**：以 `GameState` 为唯一事件源，`EventLogger` 退化为薄封装或删除；需改 engine / evaluation / UI 全部读路径
  4. **注意**：`Event.visible_to` 支持信息隔离，存档时需完整保留；事件量随局数线性增长，全量存档可接受，回放可按 `visible_to` 过滤
- **建议**：先采用方案 1 双写验证，再决定是否把 events 纳入 snapshot；**本轮不改动运行时行为**，仅记录方案
- **状态**：📋 已分析，待后续实现

## 问题 7：`src/` 架构重构（最终版）— `agent/` 包 + `core/` 纯粹化

- **范围**：`src/llm_werewolf/` 全包（约 102 个文件），含 `core/`（约 62 个）
- **总体目标**：
  1. 将 **Agent 相关能力**（创建 / 销毁 / 记忆 / 信息隔离 / 工具调用 / LLM invoke）收拢到统一 **`agent/` 包**
  2. **`core/`** 只保留游戏规则、状态、阶段编排；根目录散落文件按子目录收拢
  3. **`apps/`** 收拢 CLI；废弃 `adapter/`、`integration/`、`agents/`（复数）
- **状态**：📋 最终方案，待分 Phase 实施（**本轮不改代码**）

### 7.1 现状

**（1）Agent 代码四分五裂**

| 当前位置 | 文件 | 职责 |
|----------|------|------|
| `agents/` | `base.py`, `mixin.py` | Demo / LLM / Human 后端、`create_agent` |
| `integration/` | `agentscope.py`, `message.py` | AgentScope 主实现 |
| `adapter/` | `agent.py`, `bootstrap.py`, `factory.py` | AgentScope 再封装、开局创建与 wiring |
| `adapter/` | `information_hub.py`, `visibility.py`, `message_router.py` | MsgHub 信息隔离、通道路由 |
| `adapter/` | `bridge.py`, `structured_invoke.py`, `message.py`, `prompts.py` | LLM 调用、结构化解析、运行时 prompt |
| `core/` | `agent.py`（shim）, `phase_interaction.py` | 兼容 re-export、引擎侧 Agent 交互门面 |
| `core/` | `prompts/` 部分 | identity / selector（与 adapter prompt 重叠） |
| `core/` | `decisions.py`, `vote_intention.py` | 决策 DTO + 意向追踪（被 Hub 消费） |

**（2）`core/` 根目录平铺约 20 个 `.py`**

| 主题 | 当前文件 | 迁移去向 |
|------|----------|----------|
| 领域实体 | `game_state.py`, `player.py`, `victory.py` | → `core/domain/` |
| 事件日志 | `events.py`, `event_formatter.py`, `event_visibility.py` | → `core/runtime/events/`（**非** Agent 记忆隔离） |
| 观察构建 | `observation.py`, `decisions.py`, `phase_outputs.py` | DTO 留 core；`observation` → `core/runtime/observation/` |
| 夜晚调度 | `night_scheduler.py`, `role_night_plans.py` | → `core/rules/night/` |
| Action 元数据 | `action_registry.py`, `action_selector.py`, `death_abilities.py` | → `core/rules/` |
| 交互门面 | `phase_interaction.py` | → **`agent/facade/`** |
| 基础设施 | `serialization.py`, `locale.py`, `env.py`, `utils.py` | → `core/infra/` 或 `core/domain/snapshot/` |
| 兼容层 | `agent.py`, `role_registry.py` | **删除** / → `core/rules/roles/registry` |
| Prompt | `prompts/` 部分 | 运行时部分 → **`agent/prompts/`** |

**（3）包根**：`cli.py`、`tui.py`、`eval_cli.py`、`vote_swing_cli.py` 分散，无统一 `apps/`

→ 新人无法从目录树判断：Agent 创建 / 记忆 / 隔离 / 工具调用各在哪；core 新文件该放哪。

### 7.2 目标结构

**顶层 `src/llm_werewolf/`**

```
llm_werewolf/
├── __init__.py                 # 仅版本 + 稳定公开 API（GameEngine 等）
│
├── apps/                       # 可执行入口（薄）
│   ├── console.py              # 原 cli.py
│   ├── tui.py                  # 原 tui.py
│   ├── eval.py                 # 原 eval_cli.py
│   └── vote_swing.py           # 原 vote_swing_cli.py
│
├── agent/                      # Agent 运行时（创建→记忆→隔离→调用→销毁）
│   ├── __init__.py
│   ├── lifecycle/              # factory, bootstrap, teardown【规划】
│   ├── backends/               # base, mixin, agentscope
│   ├── memory/                 # hub, decision_log, store【规划】
│   ├── isolation/              # channels, router, policy【规划】
│   ├── invoke/                 # bridge, structured, message
│   ├── tools/                  # 【规划】空目录占位
│   ├── prompts/                # runtime, identity, manager
│   └── facade/                 # phase_interaction（引擎唯一接缝）
│
├── core/                       # 纯领域（见下）
├── ui/                         # 展示层，不变
└── evaluation/                 # 离线评测，不变
```

**`core/` 内部分层**

```
core/
├── __init__.py
├── types/                      # enums, models, protocols（含 AgentProtocol 类型定义）
├── domain/                     # player, game_state, victory, snapshot/
├── rules/                      # roles/, actions/, night/, action_registry …
├── runtime/                    # events/（日志）, observation/
├── orchestration/              # 原 engine/：phases/, action_processor, death_handler
├── config/
└── infra/                      # locale, env, utils
```

**迁移完成后删除**：`agents/`（复数）、`integration/`、`adapter/`、`core/agent.py`

### 7.3 依赖方向（强制）

```
apps        → core.orchestration + agent.facade + ui + evaluation
core        → 不 import agent / ui / evaluation（仅 types 中 AgentProtocol）
agent       → core.types + core.decisions + core.phase_outputs
              不 import core.orchestration / engine Mixin
ui          → core（只读），不 import agent
evaluation  → core + agent
```

- **引擎与 Agent 的唯一接缝**：`GameState.phase_interaction`（实现 `agent/facade/`，Protocol 可留 `core/types`）
- **信息隔离分工**：运行时 MsgHub → `agent/memory/hub.py` + `agent/isolation/`；Event 日志 `visible_to` → `core/runtime/events/visibility.py`

### 7.4 文件迁移对照表

| 现有路径 | 目标路径 |
|----------|----------|
| `agents/base.py` | `agent/backends/base.py` |
| `agents/mixin.py` | `agent/backends/mixin.py` |
| `integration/agentscope.py` | `agent/backends/agentscope.py`（合并 `adapter/agent.py`） |
| `adapter/bootstrap.py` | `agent/lifecycle/bootstrap.py` |
| `adapter/factory.py` | `agent/lifecycle/factory.py` |
| `adapter/information_hub.py` | `agent/memory/hub.py` |
| `adapter/visibility.py` | `agent/isolation/channels.py` |
| `adapter/message_router.py` | `agent/isolation/router.py` |
| `adapter/bridge.py` | `agent/invoke/bridge.py` |
| `adapter/structured_invoke.py` | `agent/invoke/structured.py` |
| `adapter/message.py` | `agent/invoke/message.py` |
| `adapter/prompts.py` | `agent/prompts/runtime.py` |
| `core/phase_interaction.py` | `agent/facade/phase_interaction.py` |
| `core/prompts/identity.py` 等 | `agent/prompts/` |
| `core/agent.py` | **删除** |
| `core/game_state.py` 等 | `core/domain/` |
| `core/events.py` 等 | `core/runtime/events/` |
| `core/night_scheduler.py` 等 | `core/rules/night/` |
| `core/engine/` | `core/orchestration/` |
| — | `agent/tools/`、`lifecycle/teardown.py` 等 **新建占位** |

### 7.5 迁移步骤（每步：移动 + re-export shim + 全量 pytest）

| 阶段 | 内容 | 风险 |
|------|------|------|
| **A0** | `docs/src-layout.md` + 创建 `agent/` 空目录骨架（含 `tools/` 占位） | 低 |
| **A1** | `agent/backends/` + `agent/lifecycle/` | 中 |
| **A2** | `agent/memory/` + `agent/isolation/` + `agent/invoke/` | 中高 |
| **A3** | `agent/facade/phase_interaction.py`；core 只依赖 Protocol | 中 |
| **A4** | `agent/prompts/`；删 `adapter/` | 中高 |
| **A5** | `apps/` 收拢 CLI；更新 `pyproject.toml` entry points | 低 |
| **C1** | `core/runtime/events/` + `core/runtime/observation/` | 中 |
| **C2** | `core/rules/night/` + action 元数据 | 中 |
| **C3** | `core/domain/` + `core/infra/` + snapshot | 中 |
| **C4** | `core/engine/` → `core/orchestration/phases/`；删 shim | 高 |
| **A7** | `tests/agent/`、`tests/core/` 镜像；import-linter 查环；删全部 shim | 中 |

建议顺序：**A0 → A1–A4（agent）→ A3 后 C1–C4（core）→ A5 → A7**

### 7.6 成功标准

- Agent 代码 **仅存在于 `agent/`**（含规划空目录）
- `core/` 根目录 ≤ 2 个文件；无 `agentscope` / `adapter` 直接 import
- 包根无散落 CLI；无 `agents/`、`integration/`、`adapter/`
- `tests/` 与 `src/` 顶层一一对应
- `grep -r "adapter\|integration\.agentscope" core/` 为零（shim 期除外）

## 问题 8：core 全量审查 — 待决策项（2026-05-24）

> 逐模块 review `core/` 时已修复若干小 bug（见「修改记录」core 第二轮 + 已修复归档 6–7）。以下涉及规则/架构，**需你确认后再动**。

| # | 问题 | 文件 / 范围 | 说明 |
|---|------|-------------|------|
| 8.1 | **`enable_sheriff` 未接入** | `config/game_config.py`、`game_state.next_phase` | ✅ 已接入：`enable_sheriff=True` 才进警长选举；默认 `False` 时 `setup_game` 标记 `sheriff_election_done=True` |
| 8.2 | **`WEREWOLF_KILLED` 信息泄露** | `event_visibility.py`、`engine/base._log_event` | ✅ 已修复：刀口仍写事件日志，`visible_to` 仅存活女巫 |
| 8.3 | **白天 PK 发言范围不一致** | `voting_phase` | ✅ 放逐 PK 对齐警长：`speakers=candidates`，`audience=alive`，`exile_pk_speech` 上下文 |
| 8.4 | **梦魇狼 / 守墓狼狼票时机** | `role_night_plans.py` | ✅ 前置 planner 仅封锁/保护；狼票统一 `run_wolf_vote_phase` |
| 8.5 | **血月使徒狼队频道** | `names.participates_in_wolf_team`、`night_phase` | ✅ 未变身排除狼讨论/投票；队友全灭后 LLM 确认变身 |
| 8.6 | **胜负判定不完整** | `victory.py` | ✅ 接入 `check_special_victory`：白狼情侣、盗贼随阵营 |
| 8.7 | **`load_game` 不完整** | `engine/base._wire_game_state` | ✅ 读档后重绑 Hub / PhaseInteraction / agent |
| 8.8 | **狼队事件可见性不统一** | `engine/action_processor.py` | 各狼角色 `_log_*` 的 `visible_to` 规则不一致（**先不动**） |
| 8.9 | **守墓人查验无持久化** | `game_state.graveyard_checked` | ✅ mirror 预言家；快照读写 |
| 8.10 | **Magician 优先级** | `roles/villager.py` | 与 Guard 同槽（**先不动**） |

- **状态**：8.1–8.7、8.9 已修复；8.8、8.10 暂缓

---

*创建时间：2026-05-23*  
*最后更新：2026-05-24（core 审查 + 问题 8）*

---

## 已修复问题（归档）

### 修复 1：`__init__.py` 导出符号缺失
- **文件**：`src/llm_werewolf/core/roles/__init__.py`
- **问题**：`__all__` 中列出了 `ROLE_CATALOG`、`RoleDefinition`、`VictoryGoal`、`get_catalog`、`get_definition`，但未实际 import，导致外部导入报错
- **修复**：补充相应导入语句
- **状态**：✅ 已修复（2026-05-24）

### 修复 2：`names.py` 阵营比较使用字符串
- **文件**：`src/llm_werewolf/core/roles/names.py`
- **问题**：`player_camp_is` 中 `player.get_camp() == camp.value` 使用字符串比较，但 `get_camp()` 返回 `Camp` 枚举；`seer_apparent_camp` 中 `Camp(target.get_camp())` 冗余包装
- **修复**：改为直接枚举比较；添加类型判断避免冗余转换
- **状态**：✅ 已修复（2026-05-24）

### 修复 3：角色 description 和 private_notes 硬编码英文
- **文件**：
  - `src/llm_werewolf/core/roles/werewolf.py`
  - `src/llm_werewolf/core/roles/villager.py`
  - `src/llm_werewolf/core/roles/neutral.py`
- **问题**：所有角色的 `description` 字段和 `private_notes` 中的提示使用英文，与项目中文提示词设计不一致
- **修复**：将所有 23 个角色的 `description` 和 `private_notes` 改为中文
- **状态**：✅ 已修复（2026-05-24）

### 修复 4：`get_camp()` 返回类型不统一
- **文件**：
  - `src/llm_werewolf/core/player.py`
  - `src/llm_werewolf/core/game_state.py`
  - `src/llm_werewolf/ui/components/game_panel.py`
  - `src/llm_werewolf/evaluation/checkers.py`
- **问题**：`PlayerProtocol.get_camp()` 已声明返回 `Camp`，但 `Player.get_camp()` 实际返回 `str`（`.value`）；`get_players_by_camp` / `count_alive_by_camp` 参数仍为 `str`，与调用方传入 `Camp.WEREWOLF` 混用
- **修复**：`get_camp()` 统一返回 `Camp` 枚举；阵营查询方法参数改为 `Camp`；调用方去掉字符串字面量
- **状态**：✅ 已修复（2026-05-24）

### 修复 5：`GameStateSnapshot` 字段不完整
- **文件**：`src/llm_werewolf/core/serialization.py`
- **问题**：存档缺少警长相关字段（`sheriff_id`、`sheriff_election_done`、`sheriff_votes`、`sheriff_tie_count`）及 `vote_tie_count`，读档后警长状态与平票计数丢失
- **修复**：扩展 `GameStateSnapshot` 并在 serialize / restore 路径读写上述字段；恢复时通过 `set_sheriff()` 同步玩家 `SHERIFF` 状态
- **状态**：✅ 已修复（2026-05-24）

### 修复 6：core 全量审查 — 小 bug 批量修复
- **范围**：`game_state`、`victory`、`env`、`utils`、`player`、`roles/base`、`roles/villager`、`actions/villager`、`engine/*`、`event_formatter`、相关测试
- **要点**：`vote_tie_count` 跨天重置、首夜 `round_number`、血月 `winner_ids`、dotenv 缓存、`load_config` 校验、`get_vote_counts` 脏票、`revive` 恢复投票权、`can_act_today` 尊重 `disabled`、预言家私密笔记、女巫 used 标志、狼讨论视角、长老存活事件、读档清 EventLogger、VOTE_INTENTION_SNAPSHOT 样式等
- **详情**：见「修改记录」→ core 代码审查与修复（第二轮）
- **状态**：✅ 已修复（2026-05-24，159 项 core 测试通过）

### 修复 7：`enable_sheriff` 接入 + 刀口仅女巫可见
- **8.1**：见「修改记录」
- **8.2**：`WEREWOLF_KILLED` 写入事件日志，`visible_to` 限制为存活女巫
- **状态**：✅ 已修复（2026-05-24）
